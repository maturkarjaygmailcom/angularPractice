// import { AbstractControl, ValidatorFn } from "@angular/forms";

// export function forbidante(control: AbstractControl): { [key: string]: any } | null {
//     return (v: RegExp): ValidatorFn => {
//         const forbidante = /admin/.test(control.value)
//         return forbidante ? { forbidante: control.value } : null
//     }

// }