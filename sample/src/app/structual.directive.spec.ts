import { StructualDirective } from './structual.directive';

describe('StructualDirective', () => {
  it('should create an instance', () => {
    const directive = new StructualDirective();
    expect(directive).toBeTruthy();
  });
});
