import { ComponentVisiableDirective } from './component-visiable.directive';

describe('ComponentVisiableDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentVisiableDirective();
    expect(directive).toBeTruthy();
  });
});
